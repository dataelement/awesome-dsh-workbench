name: Catalog candidate

on:
  pull_request:
    paths:
      - data/workbenches/**

permissions: {}

jobs:
  queue-trusted-gate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "候选变更将由 workflow_run 中的 main 分支受信任代码检查"
