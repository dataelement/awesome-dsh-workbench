# 投稿与验收（准备稿）

本指南覆盖工作台市场的投稿流程与验收要点。当前没有自动审核、在线发布或邮箱校验服务。

## 产品规范

权威的产品规范是 **DSH Desktop 工作台开发指南**，发布在 <https://dshdesktop.com/workbench/skills/workbench-development/SKILL.md>，也随应用分发。规范不移动；本仓库的投稿材料格式和验收要点与之对齐，提交材料应随规范调整。

## 流程

工作台作者在 DSH Desktop 内点「制作我的工作台」，按三步进行：

1. 阅读规范，把开发指令交给自己的 Agent。
2. Agent 把工作台装到当前设备，用户确认它出现在「我的工作台」和左侧入口，并实际打开。
3. 想投稿时，再按本页要求准备材料并提交。

**本机安装并实际验证是投稿的前提**，不是与投稿并列的另一种选择。

## GitHub 投稿渠道

1. 把业务源码放在作者仓库，确保审核者可读取指定版本。
2. 复制 [投稿材料模板](examples/submission.md)，填写真实信息与测试证据。
3. 将材料保存为 `submissions/<工作台标识>/<版本>.md`，通过 Pull Request 提交到本仓库。该路径仅为文档约定，不是正式机器协议。
4. 维护者按本页验收要点核对材料和运行行为；需要修改时更新材料和对应 commit SHA，再次验收。
5. 验收通过后由维护者收录到 `catalog/`。在线市场展示需等待服务接入；提交或合并材料本身不代表已经上线。

每个版本都需要重新验收。源代码使用**完整 commit SHA** 定位，不接受可移动的分支或 tag；如提供安装包，附下载地址及 SHA-256，并说明它与源码版本的对应关系。

## 产品内投稿

计划支持在工作台市场直接提交内容，并填写、校验邮箱。具体交互及邮箱校验方式待确定；当前请勿在公开 PR、示例或目录中提交个人邮箱。

## 投稿材料

- [ ] 场景、名称、标识、版本、作者。
- [ ] GitHub 渠道提供作者仓库及完整 commit SHA；产品内渠道提供固定版本包及可追踪的提交记录，不强制要求 GitHub 仓库。
- [ ] 提供安装步骤、可检查的源码和实际验证的 DSH Desktop 版本；安装包可追溯到提交版本，并提供 SHA-256。
- [ ] 依赖、权限、外部服务、授权及限制说明完整，不含凭据或私人业务数据。
- [ ] 本次变更、验证步骤和证据可复现。

## 验收要点

审核者按公开规范第 1–4 节核对行为：

- **§1 定义与界面边界** — 公共入口不被遮挡；`customFrame` 的根布局、绝对定位、最大化与拖拽都在宿主区域内。
- **§2 工作台、会话与工作区** — 一个工作台可关联多个会话，每个会话最多归属一个工作台；工作台与工作区是多对多。
- **§3 未绑定会话时的使用** — 无会话也立即显示业务面板；原生会话区可直接创建工作区和会话；已有会话恢复原有映射而非被覆盖。
- **§4 固定入口与切换** — 同时只有一个前台工作台；切换不停止后台任务；绑定会话精确唤起其 owner；卸载的工作台不自动重装。

**本机实际安装与个人使用**是验收的前置条件。

## 材料边界

- 投稿保留作者源码链接，不要求把业务源码复制到本仓库。
- 说明面向的工作场景、界面、依赖、兼容版本和使用方法；提供必要截图及可复现的测试步骤。
- 权限、联网服务、费用或凭据配置必须如实说明；不提交凭据、个人资料或真实业务数据。
- 作者说明自身源码、资源及依赖的授权情况；本准备仓库尚未选择项目许可证。
- 尚未支持的宿主接口、未覆盖的环境应明确写出，不以预期行为代替验证结果。

## 验收记录

审核者记录本次工作台版本、源码 commit 或固定包提交标识、包校验值、宿主版本、验证结果和未解决问题。结论使用「通过 / 待补充 / 未通过」，并写明理由。修改源码或发布新版本后重新验收。

目录收录只对已验收版本有效；最终在线发布方式及审核自动化仍待实现。
