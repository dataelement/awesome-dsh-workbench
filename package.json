{
  "name": "awesome-dsh-workbench",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run validate && npm test && npm run generate && npm run generate:check",
    "validate": "node scripts/validate-data.mjs",
    "validate:pr": "node scripts/validate-pr.mjs",
    "generate": "node scripts/generate-catalog.mjs",
    "generate:check": "node scripts/generate-catalog.mjs --check",
    "probe": "node scripts/probe-catalog.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "js-yaml": "4.3.2",
    "semver": "7.8.5",
    "sharp": "0.35.4",
    "tar": "7.5.22"
  }
}
